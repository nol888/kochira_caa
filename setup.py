#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(name="kochira_caa",
      version="0.0",
      description="more kochira services",
      author="",
      author_email="",
      url="",
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False,
      install_requires=['kochira'],
      )
